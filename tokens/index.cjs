const colors = require('./colors.cjs');
const { fontSize, fontFamily } = require('./fonts.cjs');
const boxShadow = require('./boxShadow.cjs');
const borderRadius = require('./borderRadius.cjs');

module.exports = {
  colors,
  fontSize,
  fontFamily,
  boxShadow,
  borderRadius,
};
